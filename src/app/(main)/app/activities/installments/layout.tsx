// This layout file is kept for potential future customization
// The bottom nav is now hidden via the main layout based on pathname
export default function InstallmentsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}

